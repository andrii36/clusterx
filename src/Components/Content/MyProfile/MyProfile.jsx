import React from 'react';
import withRedirect from '../../../HOC/withRedirect';

const MyProfile = (props) => {
    return(
        <div>
            Profile
        </div>
    )
}

export default withRedirect(MyProfile);